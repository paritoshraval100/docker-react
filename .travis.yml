sudo: required
services:
    - docker

before_install: 
    - docker build -t paritoshraval100/docker-react -f Dockerfile.dev .

script: 
    - docker run -e CI=true paritoshraval100/docker-react npm run test
